# CISKSPAWN 1.0.32A — Compile Fix

## Fixed
- Removed duplicate `case "scoria_project" -> advanceProject(player);` from `ScoriaEntity.java`.
- This was the Java compile error from 1.0.32.

## Removed duplicate count
- 0

## Build command
```bash
cd /Users/jean-paul/cisk-spawn-mod-1.0.32A-scoria-azerion-model-swap-compile-fix-source
chmod +x gradlew
./gradlew build
```
