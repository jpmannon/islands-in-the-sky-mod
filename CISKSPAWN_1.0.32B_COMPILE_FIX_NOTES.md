# CISKSPAWN 1.0.32B — Compile Fix

Fixed duplicate switch case in `ScoriaEntity.java`.

Changed the second `scoria_project` case to `scoria_advance_project`.

Build command:

```bash
cd /Users/jean-paul/cisk-spawn-mod-1.0.32B-scoria-azerion-model-swap-compile-fix-source
chmod +x gradlew
./gradlew build
```
